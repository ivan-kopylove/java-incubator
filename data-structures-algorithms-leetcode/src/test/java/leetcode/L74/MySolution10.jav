package leetcode.L74;

class MySolution10
{
    public boolean searchMatrix(int[][] matrix, int target) {

        int i = 0;
        int j = ? // if not 0, then what?

        while(true)
        {
            if(matrix[i][j] < target)
            {
                i++; // what for do I move the whole line?
            }
            else
            {
                j--;
            }
        }

        // no MySolutionFinal.java for this problem - this hint is too obvious
    }
}
package leetcode.L200;

class MySolution12
{
    public int numIslands(char[][] grid) {

        for(int i = 0; i < grid.length; i++) // what if there is no matrix iteration?
        {
            for(int j = 0; j < grid[0].length; j++) // why [0], not [i]?
            {
                    // what happens here?
            }
        }

        return 0;
    }
}

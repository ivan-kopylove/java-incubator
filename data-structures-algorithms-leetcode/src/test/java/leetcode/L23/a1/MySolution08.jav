package leetcode.L23;


class MySolution10
{
    public ListNode mergeKLists(ListNode[] lists) {
        
            for(int i = 0; i < lists.length; i++)
            {
                if (lists[i] != null) // when lists[i] can be null?
                {

                }
            }
        }

    }
}